/** @type {import('tailwindcss').Config} */
module.exports = {
        content: ["./views/*.{html,js,tsx,ejs}"],
        theme: {
            extend: {},
        },
        plugins: [],
    }
    // make sure to run npm tailwind:css after every CSS change